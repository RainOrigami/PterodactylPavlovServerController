@using Fluxor;
@using PavlovVR_Rcon.Models.Pavlov;
@using PterodactylPavlovServerController.Models;
@using PterodactylPavlovServerController.Services;
@using PterodactylPavlovServerController.Stores;
@using Steam.Models.SteamCommunity;
@inject PavlovRconService RconService
@inject IToastService Toast
@inject ApiKeyService ApiKey
@inject AuditService AuditService
@inject IDispatcher Dispatcher
@inject IState<PlayersState> PlayersState

<div class="me-2" style="width: 425px; display: inline-block; vertical-align: top;">
    <h3 class="mb-0">Game speed</h3>
    <small class="text-muted">
        Changes the speed of the game to the multiplier you set.
    </small>
    <EditForm Model="@speed" OnValidSubmit="@speedSubmitted" class="mt-3">
        <DataAnnotationsValidator />
        <ValidationSummary />

        <div class="mb-3">
            <label>Speed multiplier (1.0 = 100%)</label>
            <InputNumber @bind-Value="speed.Value" class="form-control" />
        </div>
        <div class="mb-3">
            <button type="submit" class="btn btn-outline-success form-control">Set</button>
        </div>
    </EditForm>
</div>

@code {
    [Parameter]
    [EditorRequired]
    public PavlovRconConnection Connection { get; set; }

    [Parameter]
    [EditorRequired]
    public ServerInfo ServerInfo { get; set; }

    private readonly GenericFloatModel speed = new();

    private async Task speedSubmitted()
    {
        try
        {
            await RconService.GameSpeed(ApiKey.ApiKey, Connection.ServerId, speed.Value!.Value);
            Toast.ShowSuccess("Game speed has been updated", "Game cheat executed");
            await AuditService.Add(Connection.ServerId, $"Updated game speed to {speed.Value!.Value}");
        }
        catch (Exception e)
        {
            Toast.ShowError(e.Message);
        }
    }
}
